# Nepal Portfolio Manager Configuration Template
# Copy this to .env and fill in your values

DEBUG=True
DATABASE_URL=sqlite:///./portfolio.db

# Generate a key for sensitive data encryption
# You can use: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here
